<h1> <%= @account.name%> </h1>
<h2>All team members</h2>
<% @profiles.each do |profile| %>
<div class="container">
  <div class="row">
      <% user = User.find(profile.user_id) %>
      <div class="col-xs-12 col-md-6">
        <%= image_tag user.picture.url(:thumb), class: "img-circle" %>
        <p> <%= user.name %> </p>
      </div>
      <div class="col-xs-12 col-md-6">
        <p> Role: <%= profile.role %> </p>
        <p> Team : <%= profile.team %> </p>
      </div>
  </div>
</div>
<% end %>
